export default function TestPage() {
    return (
        <div style={{ padding: '20px', backgroundColor: 'lightblue' }}>
            <h1>Test Page - If you see this, routing is working!</h1>
            <p>This is a simple test to verify the application is running.</p>
        </div>
    );
}
